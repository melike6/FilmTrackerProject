<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FilmTracker.Views.AddEditMoviePage"
             Title="Film Ekle / Düzenle">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <Entry x:Name="TitleEntry" Placeholder="Film Adı" />
            <Entry x:Name="DirectorEntry" Placeholder="Yönetmen" />

            <Picker x:Name="GenrePicker" Title="Tür">
                <Picker.Items>
                    <x:String>Dram</x:String>
                    <x:String>Komedi</x:String>
                    <x:String>Aksiyon</x:String>
                    <x:String>Bilim Kurgu</x:String>
                    <x:String>Korku</x:String>
                </Picker.Items>
            </Picker>

            <Entry x:Name="YearEntry"
                   Placeholder="Yıl"
                   Keyboard="Numeric" />

            <HorizontalStackLayout>
                <Label Text="İzlendi mi?" />
                <Switch x:Name="WatchedSwitch" />
            </HorizontalStackLayout>

            <Label Text="Puan" />
            <Slider x:Name="RatingSlider"
                    Minimum="0"
                    Maximum="10"
                    Value="5" />

            <Editor x:Name="NoteEditor"
                    Placeholder="Not"
                    HeightRequest="100" />

            <Button Text="💾 Kaydet"
                    Clicked="OnSaveClicked" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>