<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FilmTracker.Views.StatisticsPage"
             Title="İstatistikler">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <Label Text="📊 Film İstatistikleri"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <Frame>
                <Label x:Name="TotalMoviesLabel" />
            </Frame>

            <Frame>
                <Label x:Name="WatchedMoviesLabel" />
            </Frame>

            <Frame>
                <Label x:Name="WatchlistMoviesLabel" />
            </Frame>

            <Frame>
                <Label x:Name="FavoriteMoviesLabel" />
            </Frame>

            <Frame>
                <Label x:Name="AverageRatingLabel" />
            </Frame>

            <Frame>
                <Label x:Name="MostWatchedGenreLabel" />
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>