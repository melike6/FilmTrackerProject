<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FilmTracker.Views.MovieListPage"
             Title="FilmTracker">

    <VerticalStackLayout Padding="10" Spacing="8">

        
        <HorizontalStackLayout Spacing="10">
            <Button Text="➕ Film Ekle"
                    HorizontalOptions="FillAndExpand"
                    Clicked="OnAddMovieClicked" />

            <Button Text="📊 İstatistikler"
                    HorizontalOptions="FillAndExpand"
                    Clicked="OnStatisticsClicked" />
        </HorizontalStackLayout>

  
        <SearchBar x:Name="SearchBar"
                   Placeholder="Film veya yönetmen ara..."
                   TextChanged="OnSearchChanged" />

      
        <Picker x:Name="FilterPicker"
                SelectedIndexChanged="OnFilterChanged">
            <Picker.Items>
                <x:String>Tümü</x:String>
                <x:String>İzlenen</x:String>
                <x:String>İzlenmeyen</x:String>
                <x:String>Favoriler</x:String>
                <x:String>Watchlist</x:String>
            </Picker.Items>
        </Picker>

       
        <CollectionView x:Name="MoviesCollectionView"
                        SelectionMode="Single"
                        SelectionChanged="OnMovieSelected">

            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems Mode="Reveal">

                                <SwipeItem Text="👀"
                                           BackgroundColor="DodgerBlue"
                                           CommandParameter="{Binding .}"
                                           Invoked="OnWatchlistClicked" />

                                <SwipeItem Text="⭐"
                                           BackgroundColor="Gold"
                                           CommandParameter="{Binding .}"
                                           Invoked="OnFavoriteClicked" />

                                <SwipeItem Text="Sil"
                                           BackgroundColor="Red"
                                           CommandParameter="{Binding .}"
                                           Invoked="OnDeleteClicked" />

                            </SwipeItems>
                        </SwipeView.RightItems>

                        <Frame Margin="6" Padding="10">
                            <HorizontalStackLayout Spacing="10">

                                <VerticalStackLayout HorizontalOptions="FillAndExpand">
                                    <Label Text="{Binding Title}"
                                           FontAttributes="Bold"
                                           FontSize="16" />

                                    <Label Text="{Binding Director}"
                                           FontSize="12"
                                           TextColor="Gray" />
                                </VerticalStackLayout>

                                <Label Text="👀"
                                       IsVisible="{Binding IsInWatchlist}" />

                                <Label Text="⭐"
                                       TextColor="Gold"
                                       IsVisible="{Binding IsFavorite}" />

                            </HorizontalStackLayout>
                        </Frame>

                    </SwipeView>

                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </VerticalStackLayout>
</ContentPage>

